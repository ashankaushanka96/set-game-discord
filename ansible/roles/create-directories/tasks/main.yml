---
- name: Create main application directory
  file:
    path: "{{ component_path }}"
    state: directory
    mode: '0755'

- name: Create application logs directory
  file:
    path: "{{ component_path }}/logs"
    state: directory
    mode: '0755'
  when: create_logs_directory

